# API base URL (must match VITE_API_URL in api.ts)
# Local: leave empty (uses /api proxy via vite.config.ts)
# Production: set to your Render backend URL
VITE_API_URL=https://your-backend.onrender.com/api
